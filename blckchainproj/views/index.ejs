<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles/main.css">
  <title>Blockchain Viewer</title>
</head>

<body>
  <div class="container">
    <h1>Find out what it's worth</h1>

    <form action="/" id="form" class="form" method="POST">
      <select name="coin" class="form-select" >
        <option value="" disabled selected>Select Crypto</option>
        <option value="BTC" data-display="Select">Bitcoin</option>
        <option value="ETH">Ethereum</option>
        <option value="SOL">Solana (only valid USD)</option>
      </select>
      <select name="currency" class="form-select">
        <option value="" disabled selected>Select Currency</option>
        <option value="USD">USD</option>
        <option value="GBP">GBP</option>
        <option value="EUR">EUR</option>
      </select>
      <button type="submit" class="form-submit">Go</button>
    </form>
    <section id="cards" class="cards">
        <% if (locals.data) { %>
          <article class="card-item">
            <h2 class="card-activity">
              <%= data.symbol %> 
            </h2>
            <div class="card-info">
              <span class="card-participants">
                Last Trade: <%= data.last_trade_price %>
              </span>
              <span class="card-type">
                24hr volume: <%= data.volume_24h %>
              </span>
              <span class="card-participants">
                Price 24hrs ago: <%= data.price_24h %>
                <span class="card-number"></span>
              </span>
            </div>
          </article>
    
        <% } if (locals.error) { %>
          <div id="tag-error" class="tag-error">
            <%= error %>
          </div>
        <% } %>
        
      </section>
  </div>
</body>

</html>